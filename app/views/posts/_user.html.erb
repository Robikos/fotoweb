<div>
  <% if @user.avatar_file_name != nil %>
    <%= image_tag @user.avatar.url(:medium), class: "user_avatar" %>
  <% end %>

  <br>

  Username: <%= @user.username %><br>
  Posts: <%= @user.posts.count %><br>
  Registered at: <%= @user.created_at %><br>
  <% if @user != current_user %>
    <% if !@friendship %>
      <%= link_to "Dodaj do znajomych", user_friendships_path(@user.id), method: :post %>
    <% else %>
      Znajomi
    <% end %> 
  <% else %>
    Na bank się znacie
  <% end %>

  <br>
</div>
<br>
<br>
